<html>
   <head>
      <title>Signup for theStack!</title>
      <%= erb :includes %> 
      <style>
         #search, #loginout { display: none; }
         #login {
            width: 295px;
            height: 260px;
            padding: 10px;
            margin: 35px auto;
         }

         #about {
            width: 800px;
            padding: 10px;
            margin: 5px auto;
         }

         #about h3, #about p {
            margin-bottom: 10px;
         }

         #pw_box, #pw_box2, #user_box, #access_key, #secret_key {
            padding: 5px;
            width: 295px;
            margin-top: 5px;
         }

         .overTxtLabel {
            padding: 5px;
         }

         .formnotice {
            position: absolute;
            color: LightSlateGray;
         }

         .validation-advice {
            left: 310px;
            position: relative;
            top: 11px;
         }

      </style>
   </head>
   <body>
      <div id="cont">
         <%= erb(:header, :locals => {}) %> 

         <div id="main" class="clearfix">
            <div id="login">
               <form method="post" id="signupform">
                  <div id="advice1" class="formnotice"></div>
                  <input type="text" alt="Username" id="user_box" name="user_name" class="minLength:3 msgPos:'advice1'" />

                  <div id="advice2" class="formnotice"></div>
                  <input type="password" alt="Password" id="pw_box" name="password" class="required msgPos:'advice2'" />
                  
                  <div id="advice3" class="formnotice"></div>
                  <input type="text" alt="email address" id="pw_box" name="email" class="validate-email msgPos:'advice3'" />
                  
                  <div id="advice4" class="formnotice"></div>
                  <input type="text" alt="Amazon SimpleDB Access Key" id="access_key" name="access_key" autocomplete="off" class="required msgPos:'advice4'"/>
                  
                  <div id="advice5" class="formnotice"></div>
                  <input type="text" alt="Amazon SimpleDB Secret" id="secret_key" name="secret_key" autocomplete="off" class="required msgPos:'advice5'" />
                  
                  <input type="submit" value="sign up!" id="submit" />
                  <a href="/" id="cancel-button">nevermind...</a>
               </form>
            </div>

            <div id="about">
               <h3>Where can I find my Access Key and Secret?</h3>
               <p>Visit your <a href="https://aws-portal.amazon.com/gp/aws/developer/account/index.html?ie=UTF8&amp;action=access-key">AWS Access Identifiers page</a> and look for your Access Key and Secret.</p>
               
               <h3>Is this secure?</h3>
               <p>We will protect your key and secret and not share them with anyone. Really. And if all else fails, you can <a href="https://aws-portal.amazon.com/gp/aws/developer/account/index.html?ie=UTF8&amp;action=access-key">generate a new Access Key and Secret</a> at any time.</p>
            </div>
         </div>

         <%= erb :footer %> 
      </div>
      <script>
         $$('input').each(function(el) { new OverText(el); });
         v = new Form.Validator.Inline($('signupform'), { scrollToErrorsOnSubmit: false });
      </script>
   </body>
</html>
