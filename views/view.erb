<html>
<head>
   <title>The Stack</title>
   <link href="/style.css" media="screen" rel="stylesheet" type="text/css" />
</head>
<body>
   <div id="cont">
      <h1>the Stack</h1>
      <p>
         This is a site for uploading thoughts and such.
      </p>

      <p>
         <a href="/">Go Home ...</a>
      </p>

      <h2>View</h2>
      <h3><%= post.title %></h3>
      <div class="markdown">
         <%= post.nice_text %>
      </div>
      <div class="clearfix"></div>

      <h2>Most recent thoughts</h2>
      <ul>
      <% for item in posts %>
         <li>
            <a href="/view/<%= item[:postid] %>"><%= item[:title] %></a>  
             by <%= item.userid %>
             on <%= item.nice_date %>
         </li>
      <% end %>
      </ul>

      <%= erb :footer %> 
   </div>
</body>
</html>
