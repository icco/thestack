<html>
   <head>
      <title><%= post.title %> History > theStack!</title>
      <%= erb :includes %> 
   </head>
   <body>
      <div id="cont">
         <%= erb(:header, :locals => {:search => "" }) %> 

         <div id="main" class="clearfix">
            <% for item in posts %>
               <div class="hist">
                  <div><%= item.diff_title %></div>
                  <div><%= item.diff_text%></div>
               </div>
            <% end %>
         </div>

         <%= erb :footer %> 
      </div>
   </body>
</html>
